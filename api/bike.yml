get:
  summary: return bike object by id
  tags:
    - bike
  parameters:
    - name: bikeId
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: Returns bike object
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: object
                $ref: '../models/bike.yml'
